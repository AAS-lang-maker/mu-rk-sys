<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 核心：namespace必须等于UserMapper接口的全限定类名（包名+接口名） -->
<mapper namespace="com.music.Mapper.FriendMapper">


    <select id="selectFollowList" resultType="com.music.pojo.UserFollow">
            SELECT
            f1.UF_id AS UFId,
            f1.user_id AS userId,
            f1.follow_id AS followId,
            f1.create_time AS createTime,

            <!-- 自动计算互关状态 -->
            <!-- 如果 f2 存在（即对方也关注了我），则 isMutual 为 1，否则为 0 -->
            CASE WHEN f2.UF_id IS NOT NULL THEN 1 ELSE 0 END AS isMutual
            FROM
            user_follow f1
            LEFT JOIN
            user_follow f2
            ON f1.follow_id = f2.user_id  <!-- 对方的ID作为关注者 -->
            AND f2.follow_id = #{currentUserId} <!-- 对方关注的对象是我 -->
            WHERE
            f1.user_id = #{userId} <!-- 查找我关注的人 -->
        </select>
        <!-- 对应方法：selectFanList -->
        <select id="selectFanList" resultType="com.music.pojo.UserFollow">
                SELECT
                f1.UF_id AS UFId,
                f1.user_id AS userId,
                f1.follow_id AS followId,
                f1.create_time AS createTime,

                <!-- 自动计算互关状态 -->
                <!-- 如果 f2 存在（即我也关注了对方），则 isMutual 为 1，否则为 0 -->
                CASE WHEN f2.UF_id IS NOT NULL THEN 1 ELSE 0 END AS isMutual
                FROM
                user_follow f1
                LEFT JOIN
                user_follow f2
                ON f1.user_id = f2.follow_id <!-- 我的ID作为被关注者 -->
                AND f2.user_id = #{currentUserId} <!-- 我关注的对象是对方 -->
                WHERE
                f1.follow_id = #{userId} <!-- 查找关注我的人 -->
        </select>


</mapper>