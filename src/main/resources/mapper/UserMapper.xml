<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 核心：namespace必须等于UserMapper接口的全限定类名（包名+接口名） -->
<mapper namespace="com.music.Mapper.WebMasterLoginMapper">
    <insert id="insertAdmin" parameterType="com.music.pojo.UserInfo">
        <selectKey keyProperty="id" keyColumn="user_id" resultType="Integer" order="AFTER">
            SELECT LAST_INSERT_ID()  <!-- MySQL 获取自增主键的函数 -->
        </selectKey>
        <!-- 又让我遇到高级玩意了，主键自增策略-->
        INSERT INTO user (username, password, user_create_time,role)
        VALUES (#{username}, #{password}, #{createTime},1)
    </insert>
    <select id="selectAdmin" resultType="com.music.pojo.UserInfo">
        select user_id as id,username,password,user_create_time as createTime,role
        from music_db.user where username=#{username} and role=1
    </select>

</mapper>