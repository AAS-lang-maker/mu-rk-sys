<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 核心：namespace必须等于UserMapper接口的全限定类名（包名+接口名） -->
<mapper namespace="com.music.Mapper.CategoryMapper">
    <select id="selectAllCategoryId" resultType="java.util.List">
        select category.category_id,category.categoty_name from category where category_status=1 order by category_sort;
    </select>
    <select id="selectCategoryId" resultType="com.music.pojo.Category">
        select category.category_id,category.categoty_name from category where category_status=1 and category_id=#{categoryId};
    </select>
</mapper>